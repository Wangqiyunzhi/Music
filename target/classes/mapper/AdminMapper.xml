<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rabbiter.music.dao.AdminMapper">
    <resultMap id="BaseResultMap" type="com.rabbiter.music.pojo.Admin" >
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="avator" jdbcType="VARCHAR" property="avator"/>
    </resultMap>

    <select id="verifyPassword" resultType="java.lang.Integer">
        select count(*) from `admin` where name=#{username} and password=#{password}
    </select>

    <select id="selectByName" resultMap="BaseResultMap" parameterType="java.lang.String">
        select id, name, password, avator from `admin` where name=#{name} limit 1
    </select>

    <update id="update" parameterType="com.rabbiter.music.pojo.Admin">
        update `admin`
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="password != null">password = #{password},</if>
            <if test="avator != null">avator = #{avator},</if>
        </set>
        where id = #{id}
    </update>
</mapper>
