<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rabbiter.music.dao.CollectMapper">

    <resultMap id="BaseResultMap" type="com.rabbiter.music.pojo.Collect">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="type" property="type"/>
        <result column="song_id" property="songId"/>
        <result column="song_list_id" property="songListId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, `type`, song_id, song_list_id, create_time
    </sql>

    <insert id="insert" parameterType="com.rabbiter.music.pojo.Collect">
        insert into collect(user_id, `type`, song_id, song_list_id, create_time)
        values(#{userId}, #{type}, #{songId}, #{songListId}, now())
    </insert>

    <select id="existSongId" resultType="int">
        select count(*)
        from collect
        where user_id = #{userId}
          and `type` = 0
          and song_id = #{songId}
    </select>

    <select id="existSongListId" resultType="int">
        select count(*)
        from collect
        where user_id = #{userId}
          and `type` = 1
          and song_list_id = #{songListId}
    </select>

    <delete id="deleteByUserIdSongId">
        delete from collect
        where user_id = #{userId}
          and `type` = 0
          and song_id = #{songId}
    </delete>

    <delete id="deleteByUserIdSongListId">
        delete from collect
        where user_id = #{userId}
          and `type` = 1
          and song_list_id = #{songListId}
    </delete>

    <select id="collectOfUserId" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/>
        from collect
        where user_id = #{userId}
        order by create_time desc
    </select>

    <select id="collectOfUserIdAndType" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/>
        from collect
        where user_id = #{userId}
        and `type` = #{type}
        order by create_time desc
    </select>

</mapper>
